name: Package

on: push
#on:
#  push:
#    branches: [ "master" ]
#  pull_request:
#    branches: [ "master" ]

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v3

#    - uses: actions/upload-artifact@v3
#      with:
#        name: ep2
#        path: |
#          D:\a\Thief-Enhancement-Pack\Thief-Enhancement-Pack\Enhancement Pack\ep\*
    - name: Archive Release
      run: 7z a -tzip -mx=9 ep2.zip "D:\\a\\Thief-Enhancement-Pack\\Thief-Enhancement-Pack\\Enhancement Pack\\ep\\books\\"
#      uses: thedoctor0/zip-release@0.7.1
#      with:
#        type: 'zip'
#        filename: 'ep2.zip'
#        path: "D:\\a\\Thief-Enhancement-Pack\\Thief-Enhancement-Pack\\Enhancement Pack\\ep\\books\\"
    - name: Generate release tag
      id: tag
      run: |
        echo "::set-output name=release_tag::Release_$(date +"%Y.%m.%d_%H-%M")"
    - name: Create release
      uses: softprops/action-gh-release@v1
#      if: startsWith(github.ref, 'refs/tags/')
      with:
        Name: Release ${{ github.event.repository.updated_at}}
        tag_name: ${{ steps.tag.outputs.release_tag }}
        body: Latest
        files: ep2.zip